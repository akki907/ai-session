[project]
name = "crewai"
version = "0.1.0"
description = "Day demo — see ../README and ../hands-on.md"
requires-python = ">=3.12"
dependencies = [
    "crewai>=0.60.0",
    "crewai-tools>=0.8.0",
]

