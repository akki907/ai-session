[project]
name = "langgraph"
version = "0.1.0"
description = "Day demo — see ../README and ../hands-on.md"
requires-python = ">=3.12"
dependencies = [
    "langgraph>=0.0.20",
    "langchain>=0.1.0",
]

