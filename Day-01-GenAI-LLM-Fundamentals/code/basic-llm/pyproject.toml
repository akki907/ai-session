[project]
name = "basic-llm"
version = "0.1.0"
description = "Day demo — see ../README and ../hands-on.md"
requires-python = ">=3.12"
dependencies = [
    "openai>=1.40.0",
    "python-dotenv>=1.0.1",
]

